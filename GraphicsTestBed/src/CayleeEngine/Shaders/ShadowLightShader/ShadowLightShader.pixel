#include "../InputLayouts/default.hlsl"

float4 main(float depth : DEPTH) : SV_TARGET
{
  float d2 = depth * depth;
  float d3 = d2 * depth;
  float d4 = d3 * depth;

  return float4(depth, d2, d3, d4);
}
